#!/bin/dash
echo "Welcome to EVE"
mkdir -p /proc
mount -t proc proc /proc

mkdir -p /dev/pts
mount -t devpts devpts /dev/pts

mkdir -p /sys
mount -t sysfs sysfs /sys

echo !/bin/admin.1 >> /run/0.ctrl
echo /bin/user.1 >> /run/0.ctrl
echo /bin/test2 >> /run/0.ctrl
